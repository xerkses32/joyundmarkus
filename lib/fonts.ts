// Geist Mono is available via the geist npm package
// We'll use CSS @font-face in globals.css instead of next/font
// to keep consistency with other custom fonts

export const geistMono = {
	variable: '--font-geist-mono',
	className: 'font-geist-mono',
}
